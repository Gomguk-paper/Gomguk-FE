# 프로젝트 구조

FastAPI 모범 사례에 따른 프로젝트 구조입니다.

## 📁 폴더 구조

```
backend/
├── api/              # HTTP 인터페이스 (APIRouter)
│   ├── __init__.py
│   ├── recommendation.py
│   ├── papers.py
│   ├── summaries.py
│   └── user_preferences.py
├── core/             # 핵심 인프라 설정
│   ├── __init__.py
│   └── database.py   # DB 연결, 세션 생성
├── crud/             # 데이터 접근 로직 (단순 DB 쿼리)
│   ├── __init__.py
│   ├── paper.py
│   ├── summary.py
│   ├── user_action.py
│   └── user_preference.py
├── models/           # ORM 모델 정의
│   ├── __init__.py
│   ├── paper.py
│   ├── summary.py
│   ├── user_action.py
│   └── user_preference.py
├── schemas/          # Pydantic 스키마 (요청/응답)
│   ├── __init__.py
│   ├── recommendation.py
│   └── user.py
├── services/         # 비즈니스 로직
│   ├── __init__.py
│   └── recommendation_service.py
├── main.py           # FastAPI 앱 진입점
├── crawler.py        # arXiv 크롤링
├── selector.py       # 논문 선별
├── summarizer.py     # 요약 생성
└── pipeline.py       # 파이프라인 실행
```

## 📋 각 디렉토리 역할

### `api/` - HTTP 인터페이스
- **역할**: 요청을 어떻게 받을지 정의
- APIRouter를 사용해 API 엔드포인트 정의
- 요청(Request)과 응답(Response)을 담당
- 비즈니스 로직이나 DB 쿼리는 직접 작성하지 않음

### `core/` - 핵심 인프라 설정
- **역할**: 서버가 돌아가기 위한 기본 설정
- DB 연결 설정 (engine)
- 세션 생성 (SessionFactory)
- FastAPI Dependency (get_db)
- 도메인과 무관한 공통 기반 코드

### `crud/` - 데이터 접근 로직
- **역할**: DB에서 어떻게 가져올지 정의
- 실제 DB 조회, 저장, 수정 로직
- Session을 받아 쿼리 수행
- API 계층과 DB 계층을 분리

### `models/` - ORM 모델 정의
- **역할**: DB 구조가 어떻게 생겼는지 정의
- SQLAlchemy ORM 모델 정의
- DB 테이블 구조를 코드로 표현
- 데이터 스키마 책임만 가짐

### `schemas/` - Pydantic 스키마
- **역할**: 요청/응답 데이터 검증
- Pydantic 모델 정의
- API 요청/응답 형식 정의

### `services/` - 비즈니스 로직
- **역할**: 복잡한 비즈니스 로직 처리
- 추천 알고리즘 등 복잡한 계산
- 여러 CRUD 작업을 조합

## 🔄 요청 흐름

```
[ Client ] (HTTP 요청)
    ↓
[ uvicorn ] (HTTP 서버)
    ↓
[ main.py ] (FastAPI 앱 시작점)
    ↓
[ api/ ] (라우팅 계층)
    ↓
[ services/ ] (비즈니스 로직)
    ↓
[ crud/ ] (데이터 접근 계층)
    ↓
[ models/ + core/ ] (ORM 모델 + DB 연결)
    ↓
[ Database ] (실제 데이터베이스)
```

## 📝 예시: 추천 API 요청 흐름

1. **Client** → `POST /api/recommendations` 요청
2. **main.py** → FastAPI 앱이 요청 수신
3. **api/recommendation.py** → `get_recommendations()` 함수 실행
4. **services/recommendation_service.py** → 추천 점수 계산 로직
5. **crud/** → DB에서 논문, 사용자 선호도, 행동 기록 조회
6. **models/** → ORM 모델을 통해 데이터 반환
7. **api/recommendation.py** → 응답 형식으로 변환하여 반환

## 🎯 구조의 장점

- ✅ 파일 하나가 너무 커지지 않음
- ✅ 역할이 명확해 유지보수 쉬움
- ✅ API / DB / 인프라가 자연스럽게 분리됨
- ✅ 서비스 규모가 커져도 확장 가능
- ✅ 테스트 작성이 용이함
